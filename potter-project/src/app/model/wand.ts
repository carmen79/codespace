export class Wand {
    wood: string;
    length: number;
    core: string;

    constructor(
        wood: string,
        length: number,
        core: string,
        ) {
            this.wood = wood;
            this.length = length;
            this.core = core;

        }
}
